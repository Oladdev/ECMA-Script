// modules is an ES6 feature that allows you to import and export code between different files.
import { add, subtract } from "./math.js";

console.log(add(10, 5));        // 15
console.log(subtract(10, 5));   // 5
